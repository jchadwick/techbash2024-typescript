{
  "scripts": {
    "clean": "rimraf .parcel-cache dist",
    "build": "run-p slides:render",
    "start": "run-p slides:serve slides:open",
    "slides:serve": "parcel index.html",
    "slides:open": "open http://localhost:1234",
    "slides:render": "parcel build index.html"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.12.0",
    "prettier": "^3.3.3",
    "reveal.js": "^5.1.0",
    "rimraf": "^6.0.1"
  }
}
